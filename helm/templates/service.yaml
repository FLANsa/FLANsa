apiVersion: v1
kind: Service
metadata:
  name: {{ include "qayd-pos.fullname" . }}
  labels:
    {{- include "qayd-pos.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "qayd-pos.selectorLabels" . | nindent 4 }}

